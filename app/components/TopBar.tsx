"use client";

export default function TopBar() {
  return (
    <header className="h-12 bg-gray-950 border-b border-gray-800 flex items-center px-4 shrink-0">
      <span className="text-white font-semibold tracking-wide text-sm">
        Command Center
      </span>
    </header>
  );
}
